import styles from "./navbar.module.css";

const Navbar = () => {
    return (
        <nav className={styles.navbar}>
            <div>Image</div>
            <div>github</div>
        </nav>
    );
};

export default Navbar;
